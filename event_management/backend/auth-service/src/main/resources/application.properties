# ? App name
spring.application.name=auth-service

# ? Server port (different from event-service)
server.port=8082

# ? Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/event_management_auth_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=viratkohli
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Optional (Hibernate auto-detects usually)
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect